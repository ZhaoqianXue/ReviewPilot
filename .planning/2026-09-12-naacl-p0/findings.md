Screening currently stores strict booleans without grounded rationales. New structured records must retain criterion and verified record excerpt; old records explicitly lack explanations. Extraction has flat and nested fields and source provenance but no field evidence. Correction must update both representations and invalidate categorization; screening correction invalidates retrieval/extraction/categorization. Examples are only deletion-protected; all mutation routes need a shared read-only gate. Sample live runs must remain bounded and distinguish cached output from newly computed output.
