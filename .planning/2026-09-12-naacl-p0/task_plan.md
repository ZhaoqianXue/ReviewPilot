# NAACL web P0
1. Inspect screening, extraction, examples, mutation boundaries (complete).
2. Implement durable record review, verified field evidence and corrections (complete).
3. Implement protected examples, isolated copies and bounded live sample reruns (complete).
4. Integrate web UI, regression tests, isolated browser end-to-end checks (complete).
Preserve prior uncommitted memory/sidebar work. No live baseline mutations. No commit/push unless asked.
