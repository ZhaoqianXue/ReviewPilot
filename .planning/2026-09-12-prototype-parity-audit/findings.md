Baseline is reviewpilot-ui.html / design/reviewpilot-ui.source.html, as defined by prior prototype parity specification. Existing July audit only covers Step 4 and cannot establish full product parity. September criteria restoration confirms prior omission. Latest P0 adds review/evidence/copies/sampling. No AGENTS.md discovered. Root planning files describe historical work and are preserved.

Prototype markup has five stage canvases but many noop controls. Its checked-in source does not actually contain inclusion/exclusion criteria; criteria approval is required by LEAD_AGENT_UI_UX_BOUNDARY and screening design/skills. Full audit must include these documented functional contracts. DESIGN_SPEC and source are inconsistent on sidebar width; design/README names a bundle path absent from tracked file list. Avoid treating an old Step 4 parity report or current green tests as proof of complete functionality.

Initial traced gaps: readonly example allowlist omits preview-nav (visible Next paper cannot act); screening exclusion list contains only relevance exclusions, not date/exact/similarity removals despite prototype View 242 excluded implying all 312−70; retrieval projection maps all retrieved files to openAccess and always sets viaInstitution=0; Help has no handler; Finalize Project and Skip only change browser catDraft flags; search setup edit is hidden behind an unlabeled + in Assistant rather than prototype Adjust search criteria. Date key and precision path under investigation.

Browser reproduction: two-paper protected example Preview on paper shows enabled Next paper at 1/2; clicking yields no UI change. Direct trace confirms readOnlyExample allowlist blocks preview-nav. Date keys are correctly adapted by FilteringAgentContract (false alarm resolved), but filtering only compares year and drops records with missing year. Keyword add/remove updates only state.setupDraft.keywords, payload retains existing search_terms and has no keyword/concept updates. Extraction failed/finalized state has Regenerate but no visible run-extraction or retry action; decision card is draft-only.

Browser prototype Step 2 confirms 312/248/70 and View 242 excluded; no eligibility criteria control in bundled version. Current ordinary project Skip Categorization displays summary/exports; Finalize Project displays Project Complete while header remains Active. Refresh check follows.

Browser confirmed refresh removes both Skip and Project Complete, returning ordinary project to category setup. Keyword auditmarker appears as chip; will inspect saved search setup. Review uses local fixture with all model calls disabled. No production project mutations.
